/**
 * Единые параметры иконок админ-панели (lucide-react, outline-стиль).
 * Один stroke-width на всю админку; размер — по контексту использования.
 * Цвет иконки наследуется из текста кнопки/ссылки (`currentColor`),
 * поэтому он всегда берётся из design-system.
 */
export const ICON_STROKE = 1.75

export const ICON_SIZE = {
  nav: 19, // пункты сайдбара
  action: 17, // иконочные кнопки в строках таблиц
  btn: 16, // иконка внутри текстовой кнопки (создать/назад/…)
  mini: 14, // мелкие управляющие кнопки (перемещение строк, галерея)
  empty: 30, // иллюстрация пустого состояния
  toast: 18, // иконки тостов
} as const
